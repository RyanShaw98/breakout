public interface Settings {

    // Frame settings
    String FRAME_TITLE = "Breakout - 1605008";
    int FRAME_WIDTH = 300;
    int FRAME_HEIGHT = 350;

    // Rectangle settings
    int START_POS_X = (FRAME_WIDTH / 2) - (FRAME_WIDTH / 10);
    int START_POS_Y = (FRAME_HEIGHT / 10) * 9;
    int REC_WIDTH = 50;
    int REC_HEIGHT = 2;

    // Circle settings




    int DELAY = 1000;
    int PERIOD = 10;



}
